class Solution {
    public int strStr(String haystack, String needle) {
        int m = haystack.length(); // Longitud de haystack
        int n = needle.length();   // Longitud de needle
        
        return -1; // Retorno temporal
    }
}
